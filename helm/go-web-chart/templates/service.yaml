apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-web-chart.fullname" . }}
  labels:
    {{- include "go-web-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-web-chart.selectorLabels" . | nindent 4 }}
